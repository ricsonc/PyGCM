print 'step: t = '+str(ABSTIME)

celldata = True #can be replaced by a function later

#for item in cell.data that is flagged
    #create grid
    #save as .png
    #open in pil
    #render in tkinter
